[package]
name = "json_parser_cli"
version = "0.1.0"
edition = "2021"

[dependencies]
bumpalo = { version = "3.16.0", features = ["collections"] }
json_parser = { path = "../json_parser" }
simd-json = "0.14.3"

[profile.release]
codegen-units = 1
lto = 'fat'
debug = true
