[package]
name = "json_parser"
version = "0.1.0"
edition = "2021"

[dependencies]
bumpalo = { version = "3.16.0", features = ["collections"] }
mimalloc = { version = "*" }

[profile.release]
codegen-units = 1
lto = 'fat'
debug = true
